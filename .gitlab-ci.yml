
image: node

before_script:
  - npm install

cache:
  paths:
    - node_modules/

pages:
  stage: deploy
  script:
    - unset CI
    - BASE_PATH=60bb5db73f0e3e0019c63edb/ npm run build
    - mv public public2
    - mv build public
  artifacts:
    paths:
      - public
  only:
    - master
  